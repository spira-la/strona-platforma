/* eslint-disable unicorn/no-empty-file */
/**
 * DatabaseService has been removed — the project now uses TypeORM via
 * @nestjs/typeorm. Repositories are injected with @InjectRepository() in
 * each service/guard that needs database access.
 *
 * This file is kept as a placeholder until all remaining imports are updated.
 */
