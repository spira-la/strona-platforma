# Nginx Configuration for LiveKit DEV (Spirala)
# Subdomain: streamdev.spira-la.com
#
# Place this file in: /etc/nginx/sites-available/streamdev.spira-la.com
# Enable with: ln -s /etc/nginx/sites-available/streamdev.spira-la.com /etc/nginx/sites-enabled/

# Upstream for LiveKit DEV
upstream livekit_dev {
    server host.docker.internal:43020;  # LiveKit WebSocket/API port (dev) — host-mapped from container :7880
}

# Upstream for HLS nginx container (DEV)
upstream nginx_hls_dev {
    server host.docker.internal:43041;  # nginx-hls-dev container port
    keepalive 32;
}

# HTTP -> HTTPS redirect
server {
    listen 80;
    listen [::]:80;
    server_name streamdev.spira-la.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# HTTPS server for LiveKit DEV
server {
    listen 443 ssl;
    listen [::]:443 ssl;
    http2 on;
    server_name streamdev.spira-la.com;

    # SSL certificates
    ssl_certificate /etc/letsencrypt/live/streamdev.spira-la.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/streamdev.spira-la.com/privkey.pem;

    # HLS streaming (served from Docker volume via nginx-hls-dev container)
    location /hls/ {
        proxy_pass http://nginx_hls_dev;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Connection "";
        # Disable buffering for low-latency live streaming
        proxy_buffering off;

        # CORS for HLS player (allow dev frontend)
        add_header Access-Control-Allow-Origin "https://dev.spira-la.com" always;
        add_header Access-Control-Allow-Methods "GET, HEAD, OPTIONS" always;
        add_header Access-Control-Expose-Headers "Content-Length, Content-Range" always;

        # Handle OPTIONS preflight
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Allow-Origin "https://dev.spira-la.com";
            add_header Access-Control-Allow-Methods "GET, HEAD, OPTIONS";
            add_header Access-Control-Max-Age 1728000;
            add_header Content-Type "text/plain; charset=utf-8";
            add_header Content-Length 0;
            return 204;
        }
    }

    # M3U8 playlists — never cache (they update every segment duration)
    location ~ /hls/.*\.m3u8$ {
        proxy_pass http://nginx_hls_dev;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Connection "";
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Access-Control-Allow-Origin "https://dev.spira-la.com" always;
    }

    # TS segments — cache very briefly for live (immutable once written)
    # max-age=2 matches segment duration — browser fetches next segment fresh
    location ~ /hls/.*\.ts$ {
        proxy_pass http://nginx_hls_dev;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Connection "";
        add_header Cache-Control "public, max-age=2" always;
        add_header Access-Control-Allow-Origin "https://dev.spira-la.com" always;
    }

    # WebSocket proxy for LiveKit
    location / {
        proxy_pass http://livekit_dev;
        proxy_http_version 1.1;

        # WebSocket headers
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Standard proxy headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts for long-lived WebSocket connections
        proxy_connect_timeout 60s;
        proxy_send_timeout 3600s;
        proxy_read_timeout 3600s;

        # Disable buffering for real-time
        proxy_buffering off;
        proxy_cache off;
    }

    # Health check endpoint
    location /health {
        proxy_pass http://livekit_dev;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
}

# Note: For TURN/STUN (UDP/TCP on ports 43021-43022), configure at firewall level
# LiveKit clients will connect directly to these ports for media
# Ensure firewall allows:
#   - UDP 43021 (RTC)
#   - TCP/UDP 43022 (TURN)
